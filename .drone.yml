---
kind: pipeline
name: build 

# Do not start any builds if we are only doing tags:
trigger:
  event:
    exclude:
      - tag

steps:

- name: build
  image: plugins/docker
  repo: reg-01.ogilvie.us/app
  registry: reg-01.ogilvie.us
  tags: 1.0

